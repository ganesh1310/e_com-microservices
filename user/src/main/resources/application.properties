#server configs
spring.application.name=user-service
server.port=8082

#postgres configurations DB Configs
spring.datasource.url=jdbc:postgresql://localhost:5432/userDB
spring.datasource.username=postgres
spring.datasource.password=Hitman#4545
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#Hibenrate Configs (for schema generation , to display sql queries in logs)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

#Email configs with SMTP
#spring.mail.host=smtp.gmail.com         --gmail uses smtp server
#spring.mail.port=587                    --gmail uses this port number
#spring.mail.username=myemail@gmail.com  --your credentials
#spring.mail.password=swordsmanship      --your credentials
#spring.mail.properties.smtp.auth=true   --must validate with username and password to connect

#loggin configs
logging.level.root=INFO
#logging.level.com.example.order=DEBUG

#eureka
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

